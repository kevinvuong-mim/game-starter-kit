/** Primary UI font — loaded via Google Fonts in index.html */
export const FREDOKA_FONT = '"Fredoka", sans-serif';

/** Secondary body font — loaded via Google Fonts in index.html */
export const NUNITO_FONT = '"Nunito Sans", sans-serif';
